name: Snyk Code Scan

on:
  push:
    branches:
      - 'main'
      - 'development'
      - 'stable'
      - 'release/v*'
      - 'feature/**'
      - 'issue/**'
    paths-ignore:
      - '.github/workflows/*'
      - '**/*.md'
      - '**/*.txt'
  pull_request:
    branches:
      - 'main'
      - 'stable'
      - 'release/v*'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  security:
    name: Snyk Code Scan
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: dafneb/.github/.github/workflows/snyk-code-scan-python.yml@6904641c48f453824bb2ce384a02055cd21cbc2e
    secrets:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
