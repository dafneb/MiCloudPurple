-r requirements-dev.txt

# Project dependencies
# This file is used to define the main dependencies for the project.
# Use the `pip install -r requirements.txt` command to install these dependencies.
requests~=2.32.4
dnspython~=2.7.0
